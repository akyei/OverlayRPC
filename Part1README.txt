README.TXT for OverlayRP
Daniel McLaughlin
David Sanvar
Alex Kyei

To run this project, we have been ignoring the run function of CORE due it its inconsistencies.

1. Run gen_weights.rb as expected
2. Run node.rb <weight file> on each node in the simulation
3. Each node will dump its routing tables in /tmp/<n#>.dump
